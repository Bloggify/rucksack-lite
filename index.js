Config.options.clientScripts = [];
Statique._routes["/_getClientScript"] = function (req, res) {
    var url = req.url;
    var fullPath = Config.root + "/plugins/client/"
    debugger;
    // Fs.createReadStream(fullPath)
};
